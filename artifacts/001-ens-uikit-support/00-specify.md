---
stage: specify
project: ens-uikit-support
repo: openzeppelin-adapters
mode: extension
status: draft
timestamp: 2026-07-03
author: aleksandr.pasevin
previous_stage: null
tags: [ens, name-resolution, capability, evm, adapter, ccip-read, conformance, cross-repo]
related_initiatives:
  - repo: openzeppelin-ui
    path: artifacts/001-ens-uikit-support
    role: Owns the TYPE SHAPE — the `NameResolutionCapability` interface, value types (`ResolutionProvenance`, `ResolvedName`, `ResolvedAddress`, `ResolutionResult`), and the closed `NameResolutionError` union in `@openzeppelin/ui-types` (locked & shipped as UIKit SF-1), plus the React hooks / validation / display / address-book / v2-UX consumers (UIKit SF-2..SF-6). This repo implements the primitives that back that shape.
---

# ENS + ENS v2 Adapter Support — Repo Specification

## Summary _(mandatory)_

This repo implements the on-chain / gateway ENS name-resolution primitives that back the UIKit's `NameResolutionCapability`. The UIKit repo owns the type shape (locked & shipped as UIKit SF-1); this repo owns the EVM implementation: forward resolution (name → address), reverse resolution (address → name with a trustworthy `forwardVerified` flag and optional avatar), the native-error → typed-union mapping that guarantees expected failures never throw, ENS v2 (L1-only: CCIP-Read + cross-chain via `coinType`) support with an EVM-specific provenance extension type and type guard, and a parameterized adapter conformance harness that proves any adapter honors the contract. The prioritized story shape is P1 (forward resolution — the MVP backing the UIKit's own P1), P2 (reverse resolution + the conformance harness), and P3 (ENS v2 + provenance extension).

## Within-Repo Scope _(mandatory)_

- **In scope (this repo):**
  - EVM implementation of `NameResolutionCapability` in `@openzeppelin/adapter-evm-core` (capability file alongside `src/capabilities/addressing.ts`): the required synchronous `isValidName` shape check, forward `resolveName`, and reverse `resolveAddress`.
  - Reverse-resolution semantics: `forwardVerified` reported as a concrete boolean (forward-verification of `forward(name) === address`) and an optional `avatarUrl`.
  - A reusable native-error → `NameResolutionError` mapping layer (viem / RPC / gateway / timeout → the closed 7-code union), so every expected failure path returns `{ ok: false }` and never throws.
  - ENS v2 support (L1-only: CCIP-Read + cross-chain via `coinType`), an EVM-specific `EnsProvenance` extension type, and an `isEnsProvenance` type guard — both exported from `@openzeppelin/adapter-evm-core` for downstream narrowing.
  - A parameterized, adapter-agnostic conformance test harness (home: `@openzeppelin/adapter-runtime-utils`) that any `NameResolutionCapability` implementation runs against to prove contract compliance.
- **Out of scope (other repos / future phases):**
  - The capability type shape itself — the interface, value types, and error union live in `@openzeppelin/ui-types` and are owned by the UIKit initiative (UIKit SF-1, already locked & shipped). This repo implements against them and does not modify them.
  - React hooks, input validation, address-display components, AddressBookWidget integration, and v2 UX plumbing — all owned by the UIKit initiative (UIKit SF-2..SF-6).
  - Non-EVM name systems (SNS on Solana, Unstoppable Domains, `.sui`, Aptos names) — non-EVM adapters (`adapter-solana`, `adapter-midnight`, `adapter-polkadot`, `adapter-stellar`) omit the capability entirely; they MUST NOT break. Normalizing other name systems under the same capability is a follow-up initiative.
  - The `provenance.external` → v2-mechanism semantic boundary — deliberately left to the UIKit dev's decision at their SF-6 Design (see Open Questions).

## User Stories _(mandatory)_

### US-1: Integrator resolves an ENS name to an address via the EVM adapter (Priority: P1)

As a UIKit / dapp integrator wiring an EVM network, I can call the adapter's name-resolution capability to turn an ENS name (e.g. `alice.eth`) into a hex address, cheaply pre-check whether an input even looks like a name (`isValidName`), and receive a typed result — a resolved address with provenance on success, or a distinct typed error code on failure — so the UIKit's address-input path can accept ENS names without any bespoke ENS code in the consumer.

**Why this priority:** Forward resolution is the single most user-valuable ENS primitive and the direct backing for the UIKit's own P1 MVP (UIKit SF-3 — high stakes, their highest external priority). A wrong forward resolution sends funds to the wrong address, so correctness here is the whole point. It stands alone: an adapter that only forward-resolves already lets a dapp accept ENS names on input, even before reverse, conformance, or v2 exist.

**Independent Test:** Point the EVM adapter at a mainnet fork (or a mocked viem public client), call `resolveName('vitalik.eth')`, and confirm it returns `{ ok: true, value: { address: '0xd8dA6BF…', provenance: … } }`; call `isValidName('vitalik.eth')` → `true` and `isValidName('0xabc…')` → `false`; call `resolveName` on a nonexistent name and confirm it returns `{ ok: false, error: { code: 'NAME_NOT_FOUND', … } }` rather than throwing. Delivers value even without US-2 / US-3 / US-4.

**Sub-features needed:** SF-1, SF-2

---

### US-2: Integrator reverse-resolves an address to a trustworthy name (+ avatar) (Priority: P2)

As a UIKit / dapp integrator, I can call the adapter to turn a hex address into the ENS name it reverse-resolves to, receive a concrete `forwardVerified` boolean telling me whether the adapter confirmed the name forward-resolves back to that address, and receive an optional avatar URL — so the UIKit display layer can render names instead of hex without risk of rendering a spoofed (forward-mismatched) name as if it were verified.

**Why this priority:** Reverse resolution backs the UIKit display path (UIKit SF-4) and is a high-legibility win, but it is not a correctness prerequisite for the input MVP — a dapp that forward-resolves on input (US-1) but shows hex on display is still usable. Reverse also carries the forward-mismatch spoofing hazard (a reverse record pointing to a name whose forward record points elsewhere), so the adapter's obligation to report `forwardVerified` honestly is the crux and warrants its own slice after forward is proven.

**Independent Test:** With the EVM adapter on a fork, call `resolveAddress` on an address with a valid, forward-consistent reverse record and confirm `{ ok: true, value: { name, forwardVerified: true, avatarUrl?, provenance } }`; call it on an address whose reverse record does not forward-verify (forward-mismatch) and confirm the adapter suppresses the name, returning `{ ok: false, error: { code: 'ADDRESS_NOT_FOUND', … } }` (Approach A — `forwardVerified` is constant-`true` on any name the adapter does return); call it on an address with no reverse record and likewise confirm `{ ok: false, error: { code: 'ADDRESS_NOT_FOUND', … } }`.

**Sub-features needed:** SF-1, SF-3

---

### US-3: Integrator (and the UIKit team) can prove any adapter honors the capability contract (Priority: P2)

As an adapter author (or the UIKit team consuming an adapter), I can run a single parameterized conformance harness against any `NameResolutionCapability` implementation and get a pass/fail verdict on the contract obligations that only an adapter can satisfy — `forwardVerified` is always a concrete boolean, expected failures return `ok: false` and never throw, deterministic inputs under stable state return structurally-equal results, and `provenance.label` is user-safe — so a broken adapter cannot silently ship and the UIKit's cross-adapter guarantees become enforceable against a real implementation.

**Why this priority:** This is this repo's commitment back to the UIKit: it makes the UIKit's SC-004 (no silent coercion of unresolved names) and SC-006 (graceful degradation) enforceable rather than aspirational. It is P2 because it can only be built once both resolution directions exist to exercise, and the resolution primitives themselves deliver value before the harness formalizes their guarantees.

**Independent Test:** Instantiate the harness against the EVM adapter and confirm all four invariant families pass; then feed it a deliberately-broken stub adapter (one that throws on `NAME_NOT_FOUND`, or returns `forwardVerified: undefined`, or emits a URL as `label`) and confirm the harness fails it with a clear per-invariant message.

**Sub-features needed:** SF-4

---

### US-4: Integrator resolves ENS v2 names with narrowable EVM provenance (Priority: P3)

As a UIKit / dapp integrator, I can resolve ENS v2 names — which are **L1-only** (via CCIP-Read off-chain gateway, or cross-chain via `coinType` per ENSIP-9/11/19) — through the same capability, receive an EVM-specific `EnsProvenance` on the result, and narrow to it downstream via an exported `isEnsProvenance` type guard — so v2 names resolve correctly (including cross-chain / chain-scoped addresses), v2 gateway failures surface as distinct typed errors, and v2 provenance is programmatically distinguishable without string-matching a display label.

**Why this priority:** ENS v2 adoption is still ramping in 2026; graceful v1 handling remains the majority case. But a v2 name that silently fails is worse than no ENS support, so v2 cannot be ignored. It is P3 because it layers additively on the v1 forward pipeline and the error-mapping foundation, and it backs the UIKit's last sub-feature (UIKit SF-6).

**Independent Test:** With the EVM adapter configured for v2, resolve a name known to resolve only via CCIP-Read and confirm success with a result whose provenance narrows true under `isEnsProvenance` and carries the v2 discriminators; simulate an unreachable gateway and confirm `{ ok: false, error: { code: 'EXTERNAL_GATEWAY_ERROR', … } }` (never a silent v1 fallback); confirm a chain-scoped result carries `scopedToNetworkId`.

**Sub-features needed:** SF-5

---

## Sub-Features _(mandatory)_

### SF-1: Native-error → NameResolutionError mapping

**Purpose:** A reusable mapping layer that converts native failures raised by the underlying resolution transport (client exceptions, RPC errors, gateway responses, timeouts) into the closed seven-code `NameResolutionError` union, so that every expected failure path across the capability returns a typed `{ ok: false }` result and never throws. It is the single place the "never throw for expected failures" contract is centralized, consumed by the forward, reverse, and v2 paths alike; only genuine programmer errors are allowed to propagate as thrown errors.

- **Language:** typescript
- **Work-type:** service
- **Stakes:** Medium
- **Delivers user story:** US-1, US-2, US-4
- **Depends on:** none (root of DAG)
- **Pipeline invocation:** `dev3 --languages=typescript --work-types=service`

**Acceptance Scenarios:**

1. **Given** a native error representing "no record exists for this name,"
   **When** it passes through the mapping layer,
   **Then** the layer returns the `NAME_NOT_FOUND` variant with its required payload, and the caller receives an `{ ok: false }` result rather than a thrown exception.

2. **Given** a native transport timeout and, separately, an off-chain gateway failure,
   **When** each passes through the mapping layer,
   **Then** the former maps to `RESOLUTION_TIMEOUT` and the latter to `EXTERNAL_GATEWAY_ERROR` — distinct codes — and any unrecognized/unclassifiable native error maps to `ADAPTER_ERROR` carrying the underlying value as an opaque cause, never surfacing an invented code outside the closed union.

---

### SF-2: Forward resolution + capability scaffold + isValidName

**Purpose:** Implements the `NameResolutionCapability` for EVM in `@openzeppelin/adapter-evm-core` (alongside the existing `addressing.ts` capability), providing the required synchronous `isValidName` shape check and the forward `resolveName` (name → address) path, returning a typed result carrying the resolved address plus base provenance, and registering the capability factory in the EVM runtime so consumers obtain it through the standard capability seam. This is the correctness core of the input path: a wrong forward result is a fund-safety failure.

- **Language:** typescript
- **Work-type:** service
- **Stakes:** High — a wrong forward resolution sends funds to the wrong address (fund-loss risk analogous to UIKit SF-3).
- **Delivers user story:** US-1
- **Depends on:** SF-1
- **Pipeline invocation:** `dev3 --languages=typescript --work-types=service`

**Acceptance Scenarios:**

1. **Given** the EVM adapter on a network whose registry holds a forward record for `alice.eth`,
   **When** a consumer calls `resolveName('alice.eth')`,
   **Then** the capability returns an `{ ok: true }` result carrying the correct hex address and a base provenance record, and a prior `isValidName('alice.eth')` returns `true` while `isValidName` on a raw hex string returns `false`.

2. **Given** the same adapter,
   **When** a consumer calls `resolveName` on a name with no forward record (and, separately, on a network where resolution is unsupported),
   **Then** the capability returns `{ ok: false }` with `NAME_NOT_FOUND` (respectively `UNSUPPORTED_NETWORK`) — never a thrown error and never a coerced/placeholder address.

3. **Given** a non-EVM adapter in the same workspace that does not implement the capability,
   **When** the workspace is type-checked and built,
   **Then** it continues to compile and its runtime is unaffected — the new capability is additive and optional at the runtime-map level.

---

### SF-3: Reverse resolution + forward-verification + avatar

**Purpose:** Adds the reverse `resolveAddress` (address → name) path to the EVM capability, reporting `forwardVerified` as a concrete boolean that reflects whether the adapter confirmed the resolved name forward-resolves back to the queried address, and surfacing an optional avatar URL when available. Expected misses (no reverse record) return a typed `{ ok: false }`. The honest reporting of `forwardVerified` is the anti-spoofing crux the UIKit display layer depends on.

- **Language:** typescript
- **Work-type:** service
- **Stakes:** High — mis-reporting `forwardVerified` (claiming verified when it is not) lets the UI render a spoofed name as trusted, a display-layer identity/fund hazard (UIKit INV-6 is Critical).
- **Delivers user story:** US-2
- **Depends on:** SF-1, SF-2 (reuses the forward path to perform forward-verification)
- **Pipeline invocation:** `dev3 --languages=typescript --work-types=service`

**Acceptance Scenarios:**

1. **Given** an address whose reverse record resolves to a name that forward-resolves back to the same address,
   **When** a consumer calls `resolveAddress(address)`,
   **Then** the capability returns `{ ok: true }` with the name, `forwardVerified: true`, an `avatarUrl` when the adapter surfaces one, and provenance.

2. **Given** an address whose reverse record resolves to a name whose forward record points to a *different* address (forward-mismatch),
   **When** a consumer calls `resolveAddress(address)`,
   **Then** the capability suppresses the mismatched name and returns `{ ok: false }` with `ADDRESS_NOT_FOUND` (Approach A — suppress-on-mismatch); the adapter never surfaces a forward-mismatched name. `forwardVerified` stays a concrete boolean, constant-`true` on any name the adapter does return (satisfying SC-003), and anti-spoofing is preserved at the UIKit SF-4 display layer (which renders truncated hex).

3. **Given** an address with no reverse record,
   **When** a consumer calls `resolveAddress(address)`,
   **Then** the capability returns `{ ok: false }` with `ADDRESS_NOT_FOUND` rather than throwing.

---

### SF-4: Adapter conformance test harness

**Purpose:** A parameterized, adapter-agnostic conformance suite (home: `@openzeppelin/adapter-runtime-utils`) that takes any `NameResolutionCapability` implementation and asserts the contract obligations only an adapter can satisfy: `forwardVerified` is always a concrete boolean, every expected failure returns `ok: false` and never throws, deterministic inputs under stable underlying state return structurally-equal results (this repo defines the deep-equal-under-cache-TTL semantics), and `provenance.label` is user-safe (this repo defines the allowlist). It is the reusable gate that other adapters (`adapter-solana`, `adapter-midnight`, and beyond) run later, and the mechanism that makes the UIKit's cross-adapter success criteria enforceable against a real adapter.

- **Language:** typescript
- **Work-type:** service
- **Stakes:** High — it defines novel correctness semantics (cache-TTL deep-equal, the label allowlist) and is the enforceable fund-safety gate; a false pass here is itself a fund-safety hole because it lets a broken adapter ship unnoticed.
- **Delivers user story:** US-3
- **Depends on:** SF-2, SF-3 (needs both resolution directions to exercise; relies on SF-1's mapping for the never-throw checks)
- **Pipeline invocation:** `dev3 --languages=typescript --work-types=service`

**Acceptance Scenarios:**

1. **Given** the conformance harness pointed at the compliant EVM adapter,
   **When** the suite runs,
   **Then** all four invariant families (concrete-boolean `forwardVerified`, never-throw expected failures, deterministic-under-stable-state idempotency, user-safe `label`) pass, and re-running with identical inputs and stable state yields identical structural results.

2. **Given** a deliberately non-compliant stub adapter (throws on an expected failure, or returns `forwardVerified: undefined`, or emits a URL / internal identifier as `label`),
   **When** the suite runs against it,
   **Then** the harness fails it with a distinct, per-invariant failure message identifying which obligation was violated — no false pass.

---

### SF-5: ENS v2 (L1-only: CCIP-Read / offchain-gateway + cross-chain via coinType) + EnsProvenance + isEnsProvenance

**Purpose:** Extends the EVM capability to resolve ENS v2 names — which are **L1-only** (ENS Labs cancelled the Namechain L2 in Feb–Mar 2026) — through CCIP-Read off-chain gateways and cross-chain resolution via `coinType` (ENSIP-9/11/19), introduces the EVM-specific `EnsProvenance` extension type carried on v2 results and the `isEnsProvenance` type guard for downstream narrowing (both exported from `@openzeppelin/adapter-evm-core`), and routes v2-specific failures through the error-mapping layer as distinct typed errors. Cross-chain (coinType-scoped) resolutions carry the network scope so the consumer can bind the address to the correct chain. The adapter-side `EnsProvenance` extension enumerates only observable mechanism values — the `'namechain'` mechanism value is dropped — while the base `ResolutionProvenance` (`label` / `external` / `scopedToNetworkId`) is unchanged, so there is no SF-1 capability-contract change. This is the additive v2 layer on top of the v1 forward pipeline; it backs the UIKit's last sub-feature.

- **Language:** typescript
- **Work-type:** service
- **Stakes:** High — a wrong v2 resolution carries the same fund-loss risk as v1 forward, and mishandling chain-scoped addresses can bind a result to the wrong chain (analogous to UIKit SF-6).
- **Delivers user story:** US-4
- **Depends on:** SF-1, SF-2
- **Pipeline invocation:** `dev3 --languages=typescript --work-types=service`

**Acceptance Scenarios:**

1. **Given** an ENS name that resolves only via CCIP-Read,
   **When** a consumer calls `resolveName` on it,
   **Then** resolution succeeds with an `{ ok: true }` result whose provenance narrows to `EnsProvenance` under `isEnsProvenance` and carries the v2 discriminators, and a chain-scoped result additionally carries its network scope.

2. **Given** a v2 resolution attempt where the CCIP-Read gateway is unreachable,
   **When** the timeout / failure elapses,
   **Then** the capability returns `{ ok: false }` with `EXTERNAL_GATEWAY_ERROR` (distinct from `NAME_NOT_FOUND`) — never a silent fallback to a stale v1-only result — and `isEnsProvenance` remains the sole supported narrowing path (no `label` string-matching).

---

## Within-Repo Dependency DAG _(mandatory)_

```text
                    ┌─────────────┐
                    │    SF-1     │  error-taxonomy mapping (service)
                    └──────┬──────┘
                           │
                           ▼
                    ┌─────────────┐
                    │    SF-2     │  forward + capability scaffold + isValidName (service)
                    └───┬─────┬───┘
                        │     │
              ┌─────────┘     └───────────┐
              ▼                           ▼
       ┌─────────────┐             ┌─────────────┐
       │    SF-3     │             │    SF-5     │  ENS v2 + EnsProvenance
       │  reverse +  │             │  + guard    │  (service)
       │  fwd-verify │             └─────────────┘
       └──────┬──────┘
              │
              ▼
       ┌─────────────┐
       │    SF-4     │  conformance harness (service)
       └─────────────┘
       (also depends on SF-1's mapping for the never-throw checks)
```

DAG is acyclic. SF-5 (v2) branches off SF-2 in parallel with SF-3 — it needs the forward pipeline and the error mapping, not reverse. SF-4 (conformance) sits after SF-3 because it must exercise both resolution directions.

## Recommended Build Order _(mandatory)_

Topological sort, with ties broken by the cross-repo unblocking sequence:

1. **SF-1 (error-taxonomy mapping)** — root of the DAG; forward, reverse, and v2 all reuse it and it centralizes the never-throw contract.
2. **SF-2 (forward + scaffold + isValidName)** — land as early as possible: it unblocks UIKit SF-3, the UIKit's P1 MVP and highest external priority.
3. **SF-3 (reverse + forward-verify + avatar)** — unblocks UIKit SF-4.
4. **SF-4 (conformance harness)** — land with / just after SF-3, so the UIKit consumes a verified adapter across UIKit SF-2..SF-5.
5. **SF-5 (ENS v2 + provenance extension + guard)** — last; additive on the v1 forward pipeline, unblocks UIKit SF-6.

Note: SF-5 has no dependency on SF-3 or SF-4, so once SF-2 is stable it can proceed in parallel with SF-3/SF-4 if capacity allows — but its external consumer (UIKit SF-6) is last in the UIKit's own order, so it is sequenced last here.

## Build Status _(mandatory)_

|       | Research | Design | Invariants | Code | Tests | Docs |
|-------|----------|--------|------------|------|-------|------|
| SF-1  | n/a      | ✅     | ✅         | ✅   | ✅    | n/a  |
| SF-2  | ✅       | ✅     | ✅         | ✅   | ✅    | ✅   |
| SF-3  | ✅       | ✅     | ✅         | ✅   | ✅    | ✅   |
| SF-4  | ✅       | ✅     | ✅         | ✅   | ✅    | ✅   |
| SF-5  | ✅       | ✅     | ✅         | ✅   | ✅    | ✅   |

**Legend:** ✅ done · ⏳ in progress · ⏸️ not started · `n/a` stage skipped per stakes

**Filling rule:** Replace ⏸️ with `n/a` for any stage a sub-feature deliberately skips per its `stakes` setting (Low = Research / Invariants / Docs are `n/a`; Medium = Research / Docs are `n/a`; High = all stages run). The per-slice stages flip cells from ⏸️ → ⏳ on entry and ⏳ → ✅ on completion. (SF-1 is Medium → Research & Docs are `n/a`; SF-2 / SF-3 / SF-4 / SF-5 are High → all stages run.)

**Last update:** *2026-07-04 — SF-5 Docs ✅ (terminal core stage — pipeline complete for SF-5, and for the whole initiative SF-1..SF-5). Extended the existing `docs/name-resolution/` set as delivered: README (v2 hook + re-scoped non-goals + Key Concepts/Safety), api-reference (new `EnsProvenance`/`isEnsProvenance`/`buildEnsProvenance`/`deriveCoinType`/`scopedNetworkId` surface verified vs `dist/index.d.mts`; `ensL1Client` option; selection-before-shape precedence; forward provenance-upgrade note; reverse-keeps-base note; SF-5 viem version pins), integration-guide (Pattern 1 `ensL1Client` wiring; new Pattern 5 read-v2-provenance + Pattern 6 L1 cross-chain; re-scoped Common Mistakes), new runnable `examples/ens-v2-resolve/`. Both Orchestrator proviso-c items covered: the gated L1-FALLBACK wiring (deferred from SF-2 Docs) and the mainnet-bound CCIP-Read provenance upgrade (D-V9). Base `ResolutionProvenance` documented unchanged (no SF-1 contract change). No source/tests/invariants touched; no commits. CHANGELOG deferred to joint commit point (changeset-driven, per SF-2/3/4 precedent). No step-back.*

**Last update:** *2026-07-04 — SF-5 Tests ✅. 70 invariant-driven tests (19 pure-provenance + 51 service), all 26 invariants (INV-1..INV-26) covered; green: 346 name-resolution / 1079 full core / 149 adapter-evm (SC-006 preserved), tsc + lint clean. INV-22 tested as DELIVERED selection-before-shape (Code drift #1); live `test.offchaindemo.eth` + full real-viem CCIP round-trip are Out of Scope (integration). NOTE: `(SF-5, Code)` cell still ⏸️ despite complete code — needs Orchestrator correction. Prior: 2026-07-03 — SF-1 Invariants ✅. 18 invariants defined (INV-1..INV-18) across Req/Res, Error Semantics, Idempotency, Side-Effect/Obs, Sensitive Data, Perf/Reuse; Auth n/a. All three Design open questions resolved: RQ-1 timeout-vs-gateway precedence → INV-10 (+ elapsedMs does NOT belong on EXTERNAL_GATEWAY_ERROR — closed union forbids it); RQ-2 elapsedMs fallback → INV-12 (-1 sentinel + caller obligation); RQ-3 programmer-error carve-out → INV-9 (closed allowlist = {RuntimeDisposedError} only). Next eligible: SF-1 Code Draft; SF-2 Research in parallel.*

**Last update:** *2026-07-03 — SF-2 Research ✅. viem go/no-go = GO for forward resolution (`getEnsAddress` via Universal Resolver — ENSIP-10 wildcard + CCIP-Read built in, zero new dep). Enumerated gaps: G1 default `strict:false` collapses gateway/resolver/no-record failures into `null` → SF-2 must run `strict:true` + classify via SF-1 mapper (load-bearing, fund-safety); G2 real UNSUPPORTED_NETWORK signal is `ChainDoesNotSupportContract` (corrects SF-1 table rows 4–5 — drift note raised); G3 no `isValidName` in viem (build via normalize+isValidEvmAddress); G4 no `external`/offchain signal on return; G5 caller-owned timeout/elapsedMs; G6 caller must normalize. Corrected forward-path class→code table supplied in Dev Notes. Next eligible: SF-2 Design; SF-1 Code Draft.*

**Last update:** *2026-07-03 — SF-2 Design ✅. Forward path = thin service over viem `getEnsAddress` with `strict:true`; injected `PublicClient` (D-A); sync UR support-check → typed `UNSUPPORTED_NETWORK` (D-B); `UnsupportedResolverProfile`→`UNSUPPORTED_NAME` (D-C); normalize up-front via constructor (D-D). Key architecture: SF-2 pre-classifies ENS resolver-semantic reverts (`ResolverNotFound`/`ResolverNotContract`→`NAME_NOT_FOUND`, `UnsupportedResolverProfile`→`UNSUPPORTED_NAME`) on its own control path via SF-1 constructors — preserving SF-1 INV-11 (mapper never emits not-found). Finalized forward-path class→code table (D-E) in the artifact: Part A (SF-2 constructors) + Part B (SF-1 mapper — the verbatim-routable table). Drift to route to SF-1 Code Draft: mapper needs Part B only, NOT the `ResolverNotFound`/`ResolverNotContract`/`UnsupportedResolverProfile` rows (corrects the earlier "marked seam"). Next eligible: SF-2 Invariants; SF-3 Research; SF-5 Research (both branch off SF-2).*

**Last update:** *2026-07-03 — SF-1 Code Draft ✅. Delivered the transport-generic mapper `mapNameResolutionError` + 4 typed constructors + `NameResolutionErrorContext` in `packages/adapter-evm-core/src/name-resolution/error-mapping.ts` (+ barrel + `src/index.ts` re-export). All INV-1..INV-18 enforced & annotated; G2 drift applied (row 4 = `ChainDoesNotSupportContract`→`UNSUPPORTED_NETWORK`). Consumed SF-2 Design's Part-B-only finalization: ENS resolver-semantic reverts (`ResolverNotFound`/`ResolverNotContract`/`UnsupportedResolverProfile`) + normalize-throw are SF-2 control-path (via SF-1 constructors), NOT mapper rows — the earlier "marked seam" was removed (dead-but-safe). Upstream sync applied: INV-18 wording clarified (permits the runtime `RuntimeDisposedError` import INV-9 needs). Verified GREEN against local `@openzeppelin/ui-types@3.1.1` via tsc paths override. OPEN (carried to SF-1 Tests): in-place `dev:local` link doesn't materialize — pnpm lockfile-fast-path keeps resolving published 3.1.0 (blocks Tests until forced re-resolve). Next eligible: SF-1 Tests (needs link materialized), SF-2 Invariants, SF-3/SF-5 Research.*

**Last update:** *2026-07-03 — SF-2 Invariants ✅. 21 invariants (INV-1..INV-21) across Req/Res (5), Error Semantics (7), Idempotency (2), Side-Effect/Obs (3), Resource/Rate (1), Sensitive Data (1), Perf/Reuse (2); Auth n/a (public read primitive — only lifecycle gate is use-after-dispose via the guard proxy). Fund-safety core formalized: `strict:true` mandatory (INV-7), no-coerced/placeholder address (INV-2), never-throw for expected failures w/ sole sanctioned throw `RuntimeDisposedError` (INV-6), total+closed classification (INV-10) w/ fixed precedence (INV-12). Design Open Qs discharged: Q1 (`ResolverNotFound` semantics) → INV-8/INV-9 (`NAME_NOT_FOUND` from BOTH null-return AND classified reverts, produced only on SF-2 control path — preserves SF-1 INV-11); Q3 (`isValidName` dot-requirement) → INV-4. Q2 (`ResolverError` offchain reclassification) carried to SF-5. Borrowed-client no-dispose ownership (INV-15), sync pre-I/O support-gate (INV-16), SF-1 INV-12 `elapsedMs` caller obligation (INV-18). Two narrower Open Qs for Code Draft (guard-proxy raises `RuntimeDisposedError`; `isValidName` normalize hot-path cost). Next eligible: SF-2 Code Draft; SF-3 Research; SF-5 Research.*

**Last update:** *2026-07-04 — SF-3 Research ✅. viem go/no-go = GO for the reverse mechanism (`getEnsName`) + avatar (`getEnsAvatar`), zero new dep — but CONDITIONAL on forward-verification. Load-bearing finding (R1/R2): viem's only reverse primitive `getEnsName` forward-verifies INSIDE the Universal Resolver (`reverseWithGateways`) and reverts `ReverseAddressMismatch` on mismatch → returns NO name (null non-strict / throw strict); the forward-resolved address is never exposed. So `getEnsName` alone can only yield a verified name (`forwardVerified` constant-true) and CANNOT satisfy SF-3 scenario 2 (surface the mismatched name with `forwardVerified:false`). Ecosystem-wide norm (ethers `lookupAddress`, ensjs `getName` verify-or-nothing too). Three candidate designs: A (getEnsName-only, folds mismatch→ADDRESS_NOT_FOUND, not scenario-2-compliant), B (raw reverse-name read + own `resolveName` verify), C (hybrid — recommended if scenario 2 is firm; reuses SF-2 `resolveName` per scope). Gaps R1–R7 enumerated; reverse class→code table supplied (ADDRESS_NOT_FOUND via existing SF-1 `addressNotFound` on control path — mapper needs no reverse change; preserves SF-1 INV-11). Avatar = separate name-keyed best-effort lookup, must be latency/failure-isolated (R4). 4 Open Qs → Design (chief: A/B/C decision + step-back note to reconcile scenario 2 with viem semantics). Next eligible: SF-3 Design; SF-4 Research (needs SF-3).*

**Last update:** *2026-07-04 — SF-5 Research ✅. viem v2 go/no-go = GO on CCIP-Read/offchain-gateway (reuse SF-2's `getEnsAddress` pipeline; `viem@2.44.4 ≥ 2.35.0` is ENSv2-ready and its `mainnet` chain already carries the new ENSv2 UR proxy `0xeeee…eeee`, zero new dep) + GO-reframed on cross-chain (`coinType`/`toCoinType`, ENSIP-9/11/19) + N/A Namechain. **PRIMARY DRIFT (route to Orchestrator): Namechain L2 was CANCELLED by ENS Labs Feb–Mar 2026 — ENSv2 is L1-only; the spec's SF-5 "Namechain L2 registry" scope/US-4/DAG have no target.** Enumerated gaps: G1 no offchain signal on return → derive `EnsProvenance.external` via `ccipRead.request` hook interception; G2 cross-chain must resolve on an L1 mainnet client (ENS "always starts on Mainnet") — tension w/ SF-2 D-A single-client injection (deepest Design decision); G3 `scopedToNetworkId`+coinType-inverse are SF-5's to build; G4 `version:v1|v2` / mechanism largely NOT observable from viem's `Address|null` → design `EnsProvenance` on observable facts only, drop `'namechain'`; G5 spec scope obsolete. Design Open Q2 RESOLVED: `ResolverError → ADAPTER_ERROR` UNCHANGED (per ENSIP-23 the gateway-failure signal is `HttpError`, already mapped; `ResolverError`=resolver-fn-reverted, origin-ambiguous → no reclassification). Open Q3 (`provenance.external`→v2-mechanism) LEFT OPEN for UIKit SF-6 Design per spec directive. Next eligible: SF-5 Design (reframe scope; decide client model + EnsProvenance field set).*

**Last update:** *2026-07-04 — SF-1 Tests ✅. 100 tests in `packages/adapter-evm-core/src/name-resolution/__tests__/error-mapping.test.ts`, all green; full package suite 782/782 (33 files), zero regressions; test-file type-level assertions typecheck clean. All 18 invariants (INV-1..INV-18) covered — no empty matrix rows; Auth/Rate/interleaving/load recorded `n/a` (pure sync leaf). Ran against the REAL local `@openzeppelin/ui-types@3.1.1` link (materialized — entry-blocker from SF-1 Code Draft Q1 is CLEARED). No code bugs found; implementation matches invariants exactly incl. row-4 G2 drift and the removed `classifyEnsResolverRevert` seam. Two test-code-only fixes (Vite `import.meta.url`→cwd for source introspection; module-level-only `let`/`var` regex) — no source touched, no upstream sync needed. Two Open Qs raised: (1) `tsconfig` excludes `*.test.ts` so `expectTypeOf` assertions aren't CI-enforced by default — for Docs/CI; (2) INV-15 depth-cap (32) semantics for the SF-4 conformance harness (disposed error beyond depth 32 → `ADAPTER_ERROR`, intended). SF-1 pipeline complete (Docs n/a at Medium). Next eligible: SF-2 Code Draft, SF-3/SF-5 Design.*

**Last update:** *2026-07-04 — SF-2 Code Draft ✅. Forward path implemented in `@openzeppelin/adapter-evm-core` as a thin service over viem `getEnsAddress` (`strict:true`, INV-7) and wired into the `adapter-evm` runtime. New files: `name-resolution/{name-validation,provenance,service}.ts` + `capabilities/name-resolution.ts` (`createNameResolution` factory, injected `PublicClient` per D-A); sync Universal-Resolver support-gate → typed `UNSUPPORTED_NETWORK` before any I/O (D-B); normalize-up-front (D-D); borrowed-client no-dispose (INV-15). Part-A resolver reverts classified on SF-2's control path (`ResolverNotFound`/`ResolverNotContract` → `NAME_NOT_FOUND`, `UnsupportedResolverProfile` → `UNSUPPORTED_NAME`, D-C), everything else delegated to SF-1 `mapNameResolutionError` (preserves SF-1 INV-11). Verified: `tsc` + build green on `adapter-evm-core` & `adapter-evm` against the real `@openzeppelin/ui-types@3.1.1` dev:local link, ESLint clean, runtime smoke OK; fully additive, non-EVM adapters untouched (SC-006). Drift D2: `createEvmPublicClient` surfaced from core for runtime wiring. Next eligible: SF-2 Tests; SF-3/SF-5 Design.*

**Last update:** *2026-07-04 — SF-2 Tests ✅. 90 invariant-driven tests in `packages/adapter-evm-core/src/name-resolution/__tests__/` across 5 files (`name-validation` 21, `provenance` 6, `service` 55, `service.normalize-backstop` 1, `name-resolution.factory` 7) + shared `fixtures.ts`; all green. Full package suite 925/925 (39 files), zero regressions; SF-1's 105-test suite unaffected. Ran against the REAL `@openzeppelin/ui-types@3.1.1` dev:local link (materialized). All INV-1..INV-21 covered — no empty matrix rows; Auth/rate/interleaving/load recorded `n/a` (public stateless read). Fund-safety core pinned: `strict:true` asserted (INV-7), null-never-coerced (INV-2), never-throw w/ sole `RuntimeDisposedError` throw (INV-6), zero-I/O support+shape gates (INV-16), borrowed-client no-dispose (INV-15). No source modified — no code bug found. Two Open Qs raised: (1) doc-vs-code divergence — foreign-realm resolver revert degrades SAFELY to `ADAPTER_ERROR` (not `NAME_NOT_FOUND`) because the switch gates `errorName` on `instanceof BaseError`; tested at actual behavior, propose rewording the Invariants Dev Note (or a Code-Draft follow-up if cross-realm precision is wanted); (2) CI doesn't typecheck `*.test.ts` (carried from SF-1) — verified clean out-of-band. Incidental: a glob `eslint --fix` prettier-normalized SF-1's `error-mapping.test.ts` (formatting-only, still green, untracked tree — flagged to dev). Next eligible: SF-2 Docs; SF-3/SF-5 Design.*

**Last update:** *2026-07-04 — SF-3 Design ✅. Reverse `resolveAddress` designed as a NEW METHOD on SF-2's `EvmNameResolutionService` — no new files, no new factory, NO SF-1 mapper change. Settled Approach A (SUPPRESS-on-mismatch): thin wrapper over viem `getEnsName` (`strict:true`) whose Universal Resolver forward-verifies INTERNALLY, so `forwardVerified` is a concrete constant `true` on the success path (D-R3). `ReverseAddressMismatch` (D-R2) + `null` + reverse-node reverts (`ResolverNotFound`/`ResolverNotContract`/`UnsupportedResolverProfile`, D-R4) + malformed-address input (D-R1) all fold to `ADDRESS_NOT_FOUND` via SF-1 `addressNotFound` on SF-3's control path (preserves SF-1 INV-11 — mapper untouched, no drift). Avatar (`getEnsAvatar`, viem defaults) is a name-keyed best-effort lookup in an isolated post-reverse try → `avatarUrl?`; any failure never fails/throws the reverse result (D-R5). Reuses D-A injected client, D-B sync UR support-gate, stateless/no-dispose, `baseEnsProvenance()`; `coinType` stays 60n (chain-scoped=SF-5). Reverse class→code table (D-R7) Part A (SF-3 constructors) + Part B (SF-1 mapper, unchanged) in the artifact. STEP-BACK (Recommended, not Critical): Approach A does not satisfy SF-3 scenario-2-as-written (surface mismatched name w/ `forwardVerified:false`); mismatch→ADDRESS_NOT_FOUND, anti-spoofing preserved at UIKit SF-4 display (truncated hex). Orchestrator owns the scenario-2 wording reconciliation via a Specify extension — Design did NOT edit the spec body. Research's "reuse SF-2 `resolveName` for verify" superseded by A (UR verifies internally; no `resolveName` sub-call). Next eligible: SF-3 Invariants; SF-4 Research (needs SF-3).*

**Last update:** *2026-07-04 — SF-3 Invariants ✅. 20 invariants (INV-1..INV-20) across Req/Res (5), Error Semantics (7), Idempotency (2), Side-Effect/Obs (3), Resource/Rate (1), Sensitive Data (1), Perf/Reuse (1); Auth n/a (public read primitive — only lifecycle gate is use-after-dispose via the guard proxy; no cross-tenant boundary — `ADDRESS_NOT_FOUND` echoes only the caller's own address). Anti-spoofing core formalized: `forwardVerified` a concrete boolean, constant-`true` on every returned name (INV-3, satisfies SC-003 / UIKit INV-6, discharges Design Open Q3); forward-mismatch NEVER surfaced — `ReverseAddressMismatch` suppressed (INV-11, Approach A). All "no usable, forward-verified reverse record" outcomes (null-return, the four address-scoped reverts, malformed-address input) fold to `ADDRESS_NOT_FOUND` on SF-3's control path via SF-1 `addressNotFound` (INV-8/INV-9) — mapper untouched, SF-1 INV-11 preserved, **no drift to SF-1**. `strict:true` mandatory (INV-7); total+closed classification (INV-10) w/ fixed precedence (INV-12); never-throw w/ sole `RuntimeDisposedError` (INV-6); zero-I/O support+address-shape gates (INV-16); borrowed-client no-dispose (INV-15). New avatar-isolation family: `getEnsAvatar` is strictly post-success, failure/latency-isolated, never affects the reverse result or error surface (INV-17), untrusted name-owner content passed verbatim + never logged (INV-19), bounded extra hops outside the elapsedMs window (INV-18), `avatarUrl` key-absent-when-undefined (INV-4). Design Open Q1 (`UnsupportedResolverProfile`→ADDRESS_NOT_FOUND) & Q2 (`ReverseAddressMismatch` fork coverage) carried to Tests. Three narrower Open Qs raised: (1) avatar-vs-determinism scoping for SF-4's deep-equal check (INV-13 caveat — assert reverse core unconditionally, avatarUrl only under stable avatar surface); (2) per-avatar deadline as SF-5/future hardening (INV-18 resource exposure); (3) cross-realm `ReverseAddressMismatch` precision (folds safely to ADAPTER_ERROR under the `instanceof BaseError` gate — INV-11 still holds; Code-Draft decides). Next eligible: SF-3 Code Draft; SF-4 Research (needs SF-3).*

**Last update:** *2026-07-04 — SF-3 Code Draft ✅. Reverse `resolveAddress` + private `tryGetAvatar` implemented as a NEW METHOD on SF-2's `EvmNameResolutionService` in `packages/adapter-evm-core/src/name-resolution/service.ts` — Approach A, thin wrapper over viem `getEnsName({strict:true})` whose UR forward-verifies internally, so `forwardVerified` is the constant literal `true` on the success path (INV-3). `ReverseAddressMismatch` + `null`-return + the three address-scoped reverts (`ResolverNotFound`/`ResolverNotContract`/`UnsupportedResolverProfile`) + malformed-address input all fold to `ADDRESS_NOT_FOUND` via SF-1 `addressNotFound` on SF-3's control path (INV-8/9/11); transport/gateway/timeout delegate to SF-1 `mapNameResolutionError` (Part B). Avatar via `getEnsAvatar` (viem defaults, `strict:true`) is post-success, failure/latency-isolated → `avatarUrl?` (conditional spread, key-absent-when-undefined, INV-4/17); never fails/throws the reverse result. Reuses D-A injected client, D-B sync support-gate, stateless/borrowed-client no-dispose (INV-13/15/16); `coinType` 60n; `elapsedMs` via `performance.now()` around the reverse call only (INV-18). CODE DECISION: kept the `instanceof BaseError` gate on the reverse `errorName` read — SYMMETRIC with SF-2's forward path — so a cross-realm `ReverseAddressMismatch` folds safely to `ADAPTER_ERROR` (INV-11 holds; degrades safely; Orchestrator-endorsed, KISS). **NO new files, NO new factory, NO SF-1 mapper change, NO design deviation, NO upstream sync.** All 20 invariants have annotated in-code guards. Verified GREEN against the real `@openzeppelin/ui-types@3.1.1` dev:local link: `tsc` clean on `adapter-evm-core` + `adapter-evm`, ESLint clean, build complete, 195/195 SF-1+SF-2 regression tests pass (zero regressions); additive, non-EVM untouched (SC-006). Three Open Qs carried to Tests (all INV-11-safe): `UnsupportedResolverProfile`/`ReverseAddressMismatch` fork fixtures + cross-realm-mismatch actual-behavior assertion. Next eligible: SF-3 Tests; SF-4 Research (needs SF-3).*

**Last update:** *2026-07-04 — SF-3 Tests ✅. 81 invariant-driven tests in `packages/adapter-evm-core/src/name-resolution/__tests__/service.reverse.test.ts`, all green; name-resolution suite 276/276 (7 files = 195 SF-1+SF-2 + 81 SF-3, **zero regressions**); full package suite 958/958 (39 files); `tsc` clean (exit 0) + ESLint clean against the real `@openzeppelin/ui-types@3.1.1` dev:local link. All 20 invariants (INV-1..INV-20) covered — no empty matrix rows; Auth/rate/interleaving/load recorded `n/a` (public stateless read). Anti-spoofing crux triple-pinned: `forwardVerified === true` on every returned name + source-grep guard (INV-3); suppress-on-mismatch — `ReverseAddressMismatch`→`ADDRESS_NOT_FOUND`, name surfaced nowhere (INV-11); avatar isolation — throw/slow/null never affects the reverse result or error surface (INV-17), `avatarUrl` key-absent-when-undefined (INV-4). Three carried Open Qs RESOLVED as tests: Q1 `UnsupportedResolverProfile`→`ADDRESS_NOT_FOUND`; Q2 `ReverseAddressMismatch` suppress-path (decoded-revert fixture); Q3 cross-realm mismatch → `ADAPTER_ERROR` (actual behavior, INV-11-safe, symmetric with SF-2). `fixtures.ts` extended additively (`getEnsName`/`getEnsAvatar` spies + `VITALIK_NAME`/`AVATAR_URL`) — SF-1/SF-2 tests unaffected. No source modified, no code bug found, no upstream sync. Two Open Qs carried forward (all downstream): avatar-vs-determinism scoping for SF-4 deep-equal; CI doesn't typecheck `*.test.ts` (verified clean out-of-band). SF-3 pipeline: Docs remaining. Next eligible: SF-3 Docs; SF-4 Research (needs SF-3).*

**Last update:** *2026-07-04 — SF-2 Docs ✅. Integrator-facing docs for the forward-resolution capability, built under `packages/adapter-evm-core/docs/name-resolution/` (README + api-reference + integration-guide + a runnable `examples/forward-resolve/`); stage artifact at `sf-2-forward-resolution/06-docs.md`. Documented AS DELIVERED: `createNameResolution(config, { publicClient })` injected-client factory (D-A), sync `isValidName` (ENSIP-15 normalize-based, not a TLD regex), `resolveName` with `strict:true`, the sync `UNSUPPORTED_NETWORK` support-gate (D-B), the `resolveName` precedence table, and the closed 7-code union — of which the forward path produces 6 (`ADDRESS_NOT_FOUND` marked reverse-only/SF-3, unreachable from `resolveName`). Scope held to SF-2: reverse (SF-3), ENS v2 / `EnsProvenance` (SF-5), and L2-bound L1-fallback (additive SF-5, not yet implemented) all excluded and pointed forward. Two Docs/CI items folded in from SF-2 Tests: (1) the API reference documents ACCURATELY that a foreign-realm resolver revert degrades SAFELY to `ADAPTER_ERROR` under the `instanceof BaseError` gate (never a wrong/coerced address, never a throw, `cause` preserved; precise code in the normal single-copy case) — proposed reword of the **Invariants** Dev Note flagged to the Orchestrator (Docs does not edit invariants); (2) CI does not typecheck `*.test.ts` (known package-wide gap carried from SF-1) recorded. No source/tests/invariants/design/specify modified; only edit outside `docs/` is this matrix flip. SF-2 pipeline COMPLETE (all six stages ✅). Next eligible: SF-3 Docs; SF-4 Research.*

**Last update:** *2026-07-04 — SF-5 Design ✅. `artifacts/001-ens-uikit-support/sf-5-ens-v2/02-design.md`. CLIENT MODEL (G2, the crux — Orchestrator-ruled ADDITIVE, not a step-back): inject a dedicated **mainnet L1 client alongside** SF-2's per-network bound client via a NEW optional `ensL1Client?` on `CreateNameResolutionOptions`; `resolveName` routes an otherwise-`UNSUPPORTED_NETWORK` (L2-bound, no-UR) resolve to L1 with `coinType = toCoinType(boundChainId)` — **D-A signature + D-B code UNCHANGED, no SF-2 SUCCESS path changes output, INV-15 intact** (D-V1). Target chain = bound network (only reading compatible with UIKit SF-1's locked `resolveName(name)` — Q2 confirmed, SF-1 stays locked; D-V2). `EnsProvenance extends ResolutionProvenance` with OBSERVABLE-ONLY facts — `system:'ens'` discriminant (NOT the stale sketch's unobservable `version`; D-V4), observed `external`, `coinType`, `scopedToNetworkId` — **base type UNCHANGED, no SF-1 contract change**; dropped `version`/`via`/`'namechain'` (D-V3, G4/G5). `external` observed via a per-call `ccipRead.request` wrapper (call-local flag, race-free; AsyncLocalStorage rejected — browser; D-V5, G1). `scopedToNetworkId` = bound `networkId` directly, set only when `coinType !== 60` — **no coinType-inverse needed** (D-V2 collapses Research G3; D-V6). `viaGateway = observed sawOffchain`, single call, NO v2→v1 silent fallback (D-V7); `ResolverError → ADAPTER_ERROR` unchanged (Open Q2 RESOLVED; D-V8). `EnsProvenance` rides ONLY the new L1 path; mainnet-bound keeps `baseEnsProvenance()` (proviso a; D-V9 — limitation flagged Open Q2-for-Invariants). Export `EnsProvenance` + `isEnsProvenance` from `@openzeppelin/adapter-evm-core`. Provisos baked in: (a) D-A/D-B code untouched; (b) SF-5 Code re-runs SF-2 full suite for zero regression; (c) L1-fallback documented in SF-5's OWN Docs. Open Q3 (`external`→v2-mechanism) LEFT OPEN for UIKit SF-6 — not pinned. Fully additive; minor release; zero new dep (viem@2.44.4). Next eligible: SF-5 Invariants.*

**Last update:** *2026-07-04 — SF-4 Research ✅. `artifacts/001-ens-uikit-support/sf-4-conformance-harness/01-research.md`. Verdict BUILD the parameterized conformance harness in `@openzeppelin/adapter-runtime-utils` (settled home — already houses the guard Proxy the never-throw/lifecycle checks lean on; add a `./conformance` subpath export; deps = `@openzeppelin/ui-types` only + `vitest` as peer; **zero concrete-adapter deps** — the compliant EVM run lives in `adapter-evm-core`'s tests, avoiding a dep cycle). This is TEST INFRA — viem-first N/A. Canonical prior art = **Reactive Streams TCK** (`a625d3a`): parameterize over a **factory + controlled substrate** (not a live instance), **invariant-numbered** test/report keys (`inv6_…`/`inv8_<code>_…`/`inv12_…`/`inv16_…`) for per-invariant SC-004 messages, **required-vs-optional skip** (feature-detect `resolveName?`/`resolveAddress?`; skip absent families as SKIPPED), and **the-TCK-tests-itself** = the seeded-defect meta-suite. RECOMMENDED SHAPE: a runner-agnostic **pure `checkConformance()` → `ConformanceReport`** core + thin vitest binding (so the SC-004 meta-test asserts on results as data, not captured runner output). **DEEP-EQUAL-UNDER-TTL decision:** compare the reverse/forward **core** (`{address,name,forwardVerified,provenance}` / `{name,address,provenance}`) with `dequal` after a normalize pre-pass; **`avatarUrl` compared ONLY when `stableAvatarSurface===true`** (resolves SF-3 INV-13 carry-in); object **identity NOT required** (memoizer ≡ re-querier). **LABEL-ALLOWLIST decision:** defense-in-depth (mirrors SF-1 classifier) = anchored allowlist `/^[A-Za-z][A-Za-z0-9 ]{0,63}$/` **+** belt-and-braces denylist (reject `://`, `/0x[0-9a-fA-F]{4,}/`, `@`, control chars, empty); the 3 canonical labels pass, all 4 SC-004 label defects fail; `labelPolicy` override exposed. **INV-8 throw-vs-return rule:** only an *actually-thrown* non-`RuntimeDisposedError` on an expected-failure vector is a violation; a *returned* `ADAPTER_ERROR` (incl. the SF-1 depth-32 folded-disposed case) is compliant; post-dispose `RuntimeDisposedError` is lifecycle, out of the INV-8 family. 5 Open Qs → Design: (1) confirm pure-checker split vs `describe.for`-only; (2) label allowlist width vs SF-5 hyphenated labels (`'ENS via CCIP-Read'`) — widen char-class or SF-5 picks hyphen-free; (3) fixture/substrate contract (abstract stubs for meta-suite + real-client-over-mock for EVM run); (4) network-switch determinism moot (network bound at factory time); (5) optional lifecycle-throw family. Next eligible: SF-4 Design.*

**Last update:** *2026-07-04 — SF-5 Design REVISED → v2 (`sf-5-ens-v2/02-design.md`, revision 2; v1 preserved at `02-design-v1.md`). Dev ruling on D-V9: **DELIVER truthful v2 provenance on the primary case.** FLIP: observed `external` + `EnsProvenance` now ride **EVERY** forward-resolution success (mainnet-bound AND the L1 cross-chain path), not only the L1 path — so a mainnet-bound CCIP-Read v2 name carries a truthful `external` and narrows under `isEnsProvenance` (delivers SC-005 / UIKit SC-007 for the most common v2 case). `resolveName` success routine unified over `(client, coinType)` (`resolveVia`); forward-success construction switches `baseEnsProvenance()`→`buildEnsProvenance()`; `viaGateway = observed sawOffchain` on both paths. Base `ResolutionProvenance` STILL UNCHANGED (additive at type level; base fields present — a superset). `baseEnsProvenance()` unmodified, now REVERSE-only (SF-3). All other decisions (D-V1 client model, D-V2 target=bound, D-V3/4/6/7/8) preserved verbatim. **STEP-BACK SUGGESTION (Recommended, in artifact): SF-2 provenance re-baseline** — SF-2's forward-success output changes `{label:'ENS',external:false}`→`{system:'ens',label,external:<observed>,coinType:60}`, so SF-2's ~6 provenance assertions need re-baselining; Orchestrator coordinates as a controlled SF-2 amendment (surfaced to dev before commit). Client-model provisos (a)/(b)/(c) carry; proviso (a) reinterpreted per ruling (upgrade ≠ regression). Open Q1 (`external`→v2-mechanism) still OPEN for UIKit SF-6. Next eligible: SF-5 Invariants (reads revised D-V9); SF-2 provenance amendment (Orchestrator-coordinated).*

**Last update:** *2026-07-04 — SF-4 Design ✅. `artifacts/001-ens-uikit-support/sf-4-conformance-harness/02-design.md`. Parameterized conformance harness designed as a NEW `./conformance` subpath in `@openzeppelin/adapter-runtime-utils` — all new files under `src/conformance/`, ONE existing file modified (`package.json`: add `./conformance` export + promote `vitest` to optional peer), guard Proxy + `runtime-*` modules UNTOUCHED, `sideEffects:false` held, zero concrete-adapter deps. SHAPE = runner-agnostic pure `checkConformance(config)→Promise<ConformanceReport>` core (`checker.ts` + one `checks/*` file per family) + thin `describeConformance()` vitest binding (only file importing vitest; awaited at top level, D-9); invariant-numbered `key`s; structural feature-detect → absent `resolveName`/`resolveAddress` families SKIPPED (SKIPPED never fails, D-10). Four design calls resolved with Orchestrator (all design-internal): (A) DROP the `dequal` dep — hand-roll ~30-line `structuralEqual` in `deep-equal.ts` (D-2); must-dos baked in — `{avatarUrl:undefined}`≡`{}` via normalize-drop-undefined (SF-3 INV-4), comparator unit-tested (`deep-equal.test.ts`). Novel INV-12 semantics pinned: normalize pre-pass drops undefined keys + `avatarUrl` unless `stableAvatarSurface` (D-4) + `error.cause` always (unknown-typed, may hold unstable native Error; D-3); object identity NOT required. (B) widened label allowlist `/^[A-Za-z][A-Za-z0-9 ]*(?:[-'][A-Za-z0-9 ]+)*$/` + 64-len guard + denylist (`://`, `0x`-hex runs, `@`, control chars, empty) + `labelPolicy` override (D-5); corpus locked — passes `'ENS via CCIP-Read'` (SF-5), fails all 4 SC-004 label defects. (C) lifecycle-throw family OUT of the 4 required families, deferred to Invariants; harness NEVER disposes → `RuntimeDisposedError` off the INV-8 surface (D-7/D-8). (D) declarative `ResolutionResult`-shaped vectors over a caller-supplied `makeCapability()` factory (fresh instance/case, caller-owned pinned substrate); Research's `expectedFailureVectors` FOLDED into per-vector `expect:{ok:false,code}` tags (D-6); abstract stubs drive the in-package seeded-defect meta-suite (SC-004: compliant ref + 1 stub/defect class, each asserts `passed:false` + correct invariant key), real-client-over-mock EVM run lives in `adapter-evm-core`. INV-8 throw-vs-return per commitment #4 (D-7). Harness never throws for adapter misbehavior (→ FAIL data); sole throw = `ConformanceConfigError` (caller programmer-error). NO drift, NO spec-body edit, NO upstream sync. 3 Open Qs → Invariants: optional lifecycle-throw family; network-switch-determinism-moot confirmation; INV-8 declared-code-mismatch reporting granularity. Next eligible: SF-4 Invariants.*

**Last update:** *2026-07-04 — SF-3 Docs ✅. `sf-3-reverse-resolution/06-docs.md`. Integrator-facing docs for the REVERSE-resolution path, EXTENDED IN PLACE into the shared `packages/adapter-evm-core/docs/name-resolution/` set (README + api-reference + integration-guide) rather than parallel files — one coherent capability reference (two methods, one error union, one client) + new runnable `examples/reverse-resolve/`. Documented AS DELIVERED: `resolveAddress` = Approach A (suppress-on-mismatch) thin wrapper over viem `getEnsName({strict:true})` whose UR forward-verifies internally ⇒ `forwardVerified` concrete boolean **constant `true`** on every returned name; forward-mismatch (`ReverseAddressMismatch`) / `null` / the 3 address-scoped resolver reverts / malformed-address input ALL fold to the single `ADDRESS_NOT_FOUND` code (mismatched name NEVER surfaced — anti-spoofing preserved at UIKit SF-4 display); avatar via `getEnsAvatar` is post-success, failure/latency-isolated best-effort (`avatarUrl?`, key-absent-when-undefined, untrusted name-owner content passed verbatim, outside the `elapsedMs` window). Made the `ResolvedName` type-vs-adapter nuance explicit in 3 places: contract PERMITS `forwardVerified:false`, this adapter NEVER emits it (steered consumers off a dead unverified-name branch). Reverse error table = `ADDRESS_NOT_FOUND`/`UNSUPPORTED_NETWORK`/`RESOLUTION_TIMEOUT`/`EXTERNAL_GATEWAY_ERROR`/`ADAPTER_ERROR` (NOT `NAME_NOT_FOUND`/`UNSUPPORTED_NAME`); symmetric `instanceof BaseError` safe-degradation note; viem@2.44.4 pin extended with `ReverseAddressMismatch` + `isNullUniversalResolverError`. Scope held to SF-3: `coinType` stays 60n; ENS v2 (`EnsProvenance`/`isEnsProvenance`/scoping) + avatar image-fetch/SSRF hardening excluded and pointed forward (SF-5 / UIKit). NO code-vs-docs discrepancy (matches SF-3 Design/Invariants/Code/Tests exactly). No source/tests/invariants/design/specify modified; only edit outside `docs/` is this matrix flip + line. Two Open Qs (both pre-existing CI/infra, non-blocking): `typecheck:tests` gap (carried SF-1→SF-2→SF-3); `docs/**/examples/*.ts` not compiled in CI. SF-3 pipeline COMPLETE (all six stages ✅). Next eligible: SF-4 Design (in progress); SF-5 Invariants.*

**Last update:** *2026-07-04 — SF-5 Invariants ✅. `sf-5-ens-v2/03-invariants.md` (reads revised D-V9). 26 invariants (INV-1..26) across Req/Res (10), Error Semantics (7), Idempotency (3), Side-Effect/Obs (2), Resource/Rate (1), Sensitive Data (1), Perf/Reuse (2); Auth n/a (public read primitive). Provenance-truthfulness core formalized as DELIVERED (not a gap): `EnsProvenance` built on EVERY forward success — mainnet-bound AND L1 cross-chain (INV-3); `external` truthful — true IFF an OffchainLookup was actually followed, observed via per-call `ccipRead.request`, never inferred (INV-9); RACE-FREE via call-scoped ephemeral client + call-local flag, no cross-call contamination (INV-18); `isEnsProvenance` narrows on always-present `system:'ens'` as the SOLE path — SC-005, never `label`-match (INV-10, INV-5); `scopedToNetworkId`=bound `networkId` IFF `coinType!==60`, absent mainnet-bound (INV-7); `viaGateway`=observed `sawOffchain` on both paths (INV-13) → NEVER silent fallback, gateway failure=`EXTERNAL_GATEWAY_ERROR`≠`NAME_NOT_FOUND`, single strict call, no v2→v1 retry (INV-14). Base `ResolutionProvenance` UNCHANGED — `EnsProvenance` a strict superset, no SF-1 contract change (INV-4, INV-26); SF-5 adds no error code, no mapper row (INV-15, D-V8). Client model additive/gated: `deriveCoinType` throw→`UNSUPPORTED_NETWORK` contained sync before I/O (INV-16), selection precedence bound→`ensL1Client`→`UNSUPPORTED_NETWORK` D-B preserved (INV-17), borrowed-client no-dispose extends to `ensL1Client`+per-call observing client (INV-21), `ensL1Client` mainnet RPC key never leaks (INV-24). SF-2 preserved invariants re-verified against the restructured `resolveName`/`resolveVia`; SF-2 INV-5 (`baseEnsProvenance`) now reverse-only (forward switches to `buildEnsProvenance`). Open Q1 (`external`→v2-mechanism) LEFT OPEN for UIKit SF-6; SF-2 provenance-test re-baseline scoped to SF-5 CODE (Design Step-Back), NOT an invariants concern. 3 narrower Open Qs → Code Draft (observing-client transport-reuse mechanism; mainnet+`ensL1Client` branch assertion; `scopedNetworkId` helper-vs-inline). No source/tests touched. Next eligible: SF-5 Code Draft.*

**Last update:** *2026-07-04 — SF-4 Invariants ✅. `sf-4-conformance-harness/03-invariants.md`. 26 invariants (INV-1..26) for the HARNESS ITSELF (subject-under-test is the conformance harness, not the adapters it grades): Req/Res (7), Error Semantics (5), Idempotency/determinism (5), Side-Effect/Obs (2), Resource/Rate (1), Sensitive Data (2), Perf/Reuse (4); Auth n/a (pure test-infra library — no auth boundary, recorded explicitly). Fidelity core: faithful enforcement of UIKit INV-6 concrete-boolean `forwardVerified` (INV-4), UIKit INV-8 never-throw taxonomy incl. full decision table (INV-8), UIKit INV-12 determinism via normalize+`structuralEqual` w/ conformance-critical comparator/normalizer unit-proof (INV-13/14/15), UIKit INV-16 label allowlist+denylist corpus-locked (INV-5). Hygiene core: harness NEVER throws for adapter misbehavior — sole throw is `ConformanceConfigError` (INV-10); total exception containment across ALL vectors (INV-9); pure/side-effect-free (INV-18); zero-concrete-adapter-deps + runner-free core + quarantined vitest (INV-23); bounded deterministic work (INV-20); self-determinism (INV-16); SC-004 seeded-defect meta-suite = 100% detection w/ correct per-invariant keys (INV-25). **3 Design Open Qs RESOLVED:** Q1 optional lifecycle-throw family → ADMITTED as INV-26 (opt-in, default-SKIPPED, isolated dedicated instance — completes the throw taxonomy while preserving D-8 purity; Code-Draft may defer impl); Q2 network-switch determinism → confirmed NON-obligation (INV-13; network binds at factory time); Q3 INV-8 declared-code-mismatch → in-union-but-wrong code = PASS-with-note, out-of-union code = FAIL (INV-8 table). **2 design gaps closed locally (flagged, NOT step-backs):** total exception containment widened to success-vectors too (INV-9 — design scoped try/catch to ok:false only; a success-vector throw would otherwise reject checkConformance, breaking "misbehavior is data"); vector-expectation fidelity (INV-6 — declared-ok:true returning {ok:false} now FAILs + SKIPs dependent checks instead of slipping). Extension mode: only `package.json` modified; guard Proxy + runtime-* untouched, `sideEffects:false` held. No spec-body edit, no upstream drift, no source touched. 3 Open Qs → Code Draft (INV-6 mismatch severity confirm; INV-26 ship-now-vs-defer; INV-8 mismatch machine-filterable signal deferred). Next eligible: SF-4 Code Draft.*

**Last update:** *2026-07-04 — SF-4 Code Draft ✅. `sf-4-conformance-harness/04-code-draft.md`. Built the parameterized conformance harness as a NEW `./conformance` subpath in `@openzeppelin/adapter-runtime-utils` — 13 new source files under `src/conformance/` (runner-agnostic pure `checkConformance(config)→Promise<ConformanceReport>` core: `checker.ts` + `deep-equal.ts` + `label-policy.ts` + `internal.ts` + `types.ts` + one `checks/*` per family [never-throws/forward-verified/determinism/label-user-safe/lifecycle] + thin `describeConformance()` `vitest-binding.ts` + `index.ts`) plus 4 test files; ONE existing file modified (`package.json`: `./conformance` export + `vitest` promoted to OPTIONAL peer). Guard Proxy + `runtime-*` + `.` entry UNTOUCHED, `sideEffects:false` held, ZERO concrete-adapter deps (ui-types only; vitest quarantined to binding + `__tests__`). Enforces UIKit INV-6/8/12/16 + optional INV-26; all 26 SF-4 invariants have in-code guards. Dev calls implemented as confirmed: INV-6 vector-expectation mismatch = **EXPECT FAIL + dependent SKIP**; INV-26 lifecycle family **SHIPPED** (opt-in `lifecycleProbe`, default-absent, dedicated disposed instance — cannot touch required-four verdict); INV-8 in-union-wrong-code = **PASS-with-note** (no new CheckStatus). Novel semantics pinned: hand-rolled `structuralEqual` + normalize (undefined-key≡absent, `avatarUrl` only under `stableAvatarSurface`, `error.cause` always dropped) — unit-tested; widened allowlist `/^[A-Za-z][A-Za-z0-9 ]*(?:[-'][A-Za-z0-9 ]+)*$/` + 64-len + denylist + `labelPolicy` override — corpus locked (`'ENS via CCIP-Read'` passes, 4 SC-004 defects fail). Closed 7-code union compile-time-pinned to `NameResolutionError['code']` (drift breaks build). Single `invoke()` wrapper (INV-9 across ALL vectors incl. success), single `isRuntimeDisposedError` (INV-11, instanceof+name), no-leak `describeError` (INV-12). Minor refinements (forward-consistent, noted): key derivation centralized in checker for INV-3 uniqueness; `InvariantId` +`'EXPECT'`/`'INV-26'`; `ConformanceConfig.lifecycleProbe?` added. SC-004 meta-suite = compliant ref PASS + 1 stub/defect FAIL with correct key (100% detection). Verified GREEN on real `@openzeppelin/ui-types@3.1.1` dev:local link: `tsc` clean, ESLint clean, **8 files / 87 passed / 5 skipped, zero regressions**. Compliant-EVM RUN stays in adapter-evm-core's OWN tests (dep-cycle avoidance) — NOT this package. ⚠️ Env note: a stray `pnpm install` (triggered by `pnpm typecheck`) reverted the ui-types dev:local overlay to registry 3.1.1 across the 3 ENS packages; RESTORED by re-pointing the symlinks to the intact tgz overlay (no reinstall, no tracked-file change) — concurrent agents should verify via `./node_modules/.bin/tsc`/`vitest` directly or re-run `pnpm dev:local`. No SF-1/2/3 source touched, no spec-body edit, no upstream drift. 2 Open Qs → Tests (finer binding projection-count spy; adapter-evm-core compliant-run wiring). Next eligible: SF-4 Tests.*

**Last update:** *2026-07-04 — SF-4 Tests ✅. `sf-4-conformance-harness/05-tests.md`. Invariant-driven verification of the HARNESS ITSELF against all 26 SF-4 invariants — 7 NEW test files (+1 test-only helper `harness-fixtures.ts`) under `src/conformance/__tests__/` on top of the Code-stage seeded suite; **NO source modified** (harness exercised as shipped, no bug found). Closed the gaps the Code suite left: report shape/exactly-once coverage/key-prefix+collision-dedup (INV-1/2/3); `forwardVerified` `typeof`-not-truthiness boundary incl. `false`→PASS (INV-4); forward-direction EXPECT-FAIL+SKIP (INV-6); full INV-8 decision table incl. missing/non-string code + disposed→SKIP (INV-8); three INV-9 containment call sites (2nd-call throw→INV-12-not-INV-8, ctor throw, fwd-success throw); 20-row `ConformanceConfigError` validation table + before-any-call + `code` (INV-10); `isRuntimeDisposedError` predicate incl. cross-realm + totality (INV-11); no-leak `describeError` custom-class (INV-12); determinism identity-vs-equality/avatar-surface/failure-vector + cause-blindness (INV-13/21); comparator reflexive/symmetric (INV-15); self-determinism ORDERING (INV-16); instrumented fresh-instance/zero-dispose (INV-17) + bounded 2-calls/vector-no-retry (INV-20) via a spy factory; console-spy side-effect-freedom (INV-18); static import-graph scan = ui-types-only core + quarantined vitest (INV-23); non-EVM pluggability + closed-code-set (INV-24); each denylist rule + allowlist-primary ordering (INV-5) + mutation-THROWS immutability (INV-7); INV-26 three fail branches + byte-identical required-family isolation; SC-004 TIGHTENED — each single-defect stub FAILs EXACTLY its own invariant (INV-25). Verified GREEN on real `@openzeppelin/ui-types@3.1.1` dev:local link (verified intact before+after; all via `./node_modules/.bin/tsc`+`vitest`+root `eslint`, NEVER bare `pnpm` — overlay-revert hazard): `tsc --noEmit` clean, ESLint `--max-warnings 0` clean, **conformance 11 files / 172 passed / 5 skipped (+109 net-new); full package 14 files / 186 passed / 5 skipped, ZERO regressions**. One documented limit: INV-19 fine-grained emission spy blocked by `vitest` ES-module-namespace immutability (`vi.spyOn`+reassign both rejected in-process) — projection proven END-TO-END for PASS/SKIP + config-error path; residual → OOS + Recommended step-back (additive runner-injection seam on `describeConformance`). Compliant-EVM RUN stays in adapter-evm-core's OWN Tests (dep-cycle). No spec-body edit beyond this matrix flip+line, no upstream drift. 2 Open Qs → Docs/downstream (INV-19 runner seam; adapter-evm-core compliant-run wiring). SF-4 pipeline: Docs remaining. Next eligible: SF-4 Docs; SF-5 Code Draft.*

**Last update:** *2026-07-04 — SF-4 Docs ✅. `sf-4-conformance-harness/06-docs.md`. **SF-4 pipeline COMPLETE (full High-stakes pipeline).** Adapter-author integration docs for the `@openzeppelin/adapter-runtime-utils/conformance` subpath, built under `packages/adapter-runtime-utils/docs/conformance/`: `README.md` (quick start + key concepts + safety), `api-reference.md` (every export w/ full TS signature drawn from source — incl. the `EXPECT`/`INV-26` `InvariantId` members + `lifecycleProbe` flag the Code stage added beyond the design's four; the six report-key shapes; the closed 7-code set), `integration-guide.md` (5 patterns: concrete-adapter `makeCapability()` wiring / runner-agnostic CI gate / feature-detect SKIP / custom `labelPolicy` / opt-in lifecycle probe; INV-8 decision table; common mistakes; known limitations; maintainer SC-004 note), and 2 runnable `examples/` (adapter-conformance.test.ts + ci-gate.ts). Headline pattern documented in 3 places: the compliant conformance RUN lives in `adapter-evm-core`'s OWN tests (dep-cycle avoidance, zero concrete-adapter deps) — copy-paste factory wiring supplied. Deep-equal-under-TTL, allowlist+denylist+override, required-vs-optional SKIP-≠-PASS, and the seeded-defect 100%-detection story all covered. Both example modules compiled clean with the package's own `./node_modules/.bin/tsc` against real `@openzeppelin/ui-types@3.1.1` dev:local (caught+fixed 2 example shape bugs while drafting: `ResolvedAddress` needs `name`+`provenance.external`; `NAME_NOT_FOUND` error is `{code,name}`). **No code-vs-docs discrepancy** — API ref written from source, matches design/invariants/tests exactly. Direct-binary discipline honored (no bare `pnpm`, overlay intact); NO source/test file modified. 2 Open Qs carried as documented items: (1) INV-19 fine-grained emission-spy residual → known limitation + Recommended non-blocking additive runner-injection seam on `describeConformance`; (2) `adapter-evm-core` compliant-EVM run wiring → known downstream follow-up (out of this package's scope). No spec-body edit beyond this matrix flip+line, no upstream drift. Next eligible: SF-5 Code Draft.*

**Last update:** *2026-07-04 — SF-5 Code Draft ✅. ENS v2 (L1-only) forward path delivered: `EnsProvenance` extension + `isEnsProvenance` guard, `resolveVia` / `deriveObservingClient` (observed `external` via the per-call `ccipRead.request` hook), and the gated optional `ensL1Client`; the dev-approved SF-2 provenance re-baseline landed with resolution behavior unchanged and zero regression (346 name-resolution / 1079 core / 149 adapter-evm green). Matrix reconciliation: the `(SF-5, Code)` cell flip had been clobbered by a concurrent SF-4 Docs write — corrected here. Next eligible: SF-5 Docs.*

**Last update:** *2026-07-03 — SF-1 Design ✅ (backfilled 2026-07-04 — entry was missing from the log). Designed the reusable, **stateless** native-error → `NameResolutionError` mapping module in `@openzeppelin/adapter-evm-core`: one total function `mapNameResolutionError` + a small set of typed "resolved-to-nothing" constructors + `NameResolutionErrorContext`, converting viem / RPC / CCIP-Read-gateway / timeout failures into the closed 7-code union locked by UIKit SF-1 — classification + union construction only (no state, no I/O). Follows the `erc4626` domain-directory pattern; SF-2/SF-3/SF-5 build the rest of `src/name-resolution/` into the same dir. Next eligible: SF-1 Invariants.*

## Success Criteria _(mandatory)_

- **SC-001**: An integrator wiring the EVM adapter can forward-resolve a well-known ENS name to the correct hex address through the capability with no bespoke ENS code in the consumer — verified against a public test set (e.g. `vitalik.eth`, `nick.eth`, top-100 registrations) with ≥95% forward-resolving correctly in an integration environment.
- **SC-002**: 100% of expected failure paths (name not found, address not found, unsupported network, unsupported name, timeout, external-gateway error) return a typed `{ ok: false }` with a distinct code from the closed seven-code union — zero expected failures propagate as thrown exceptions, verified by the conformance harness.
- **SC-003**: Every `resolveAddress` result carries `forwardVerified` as a concrete boolean (never `undefined`), verified by the conformance harness across compliant and non-compliant adapters.
- **SC-004**: The conformance harness fails every deliberately non-compliant adapter it is fed (throw-on-expected-failure, `undefined` `forwardVerified`, non-user-safe `label`, non-deterministic-under-stable-state) with a per-invariant message, and passes the compliant EVM adapter — a 100% detection rate on the seeded-defect set.
- **SC-005**: ENS v2 provenance on a v2 result narrows correctly under `isEnsProvenance` (type guard returns `true` for v2 results and `false` for base/non-EVM provenance), and no consumer needs to string-match `provenance.label` to detect v2.
- **SC-006**: The non-EVM adapters (`adapter-solana`, `adapter-midnight`, `adapter-polkadot`, `adapter-stellar`) continue to type-check, build, and pass their existing tests unchanged after this initiative lands — zero regressions, the capability being additive and optional at the runtime-map level.

## Assumptions _(mandatory if any defaults were taken; remove section if none)_

- **Integration is local workspace-linking, not npm publish.** There is no publish/consume step during development. The UIKit repo ships `pnpm dev:local` (backed by `oz-ui-dev use local --family adapters` in its `packages/dev-cli`) which points a consumer app at both local checkouts (`../openzeppelin-adapters` + the UIKit repo) via `.openzeppelin-dev.json` + a `.pnpmfile.cjs` hook. Both repos run their full pipelines against local-linked integration; a single joint npm publish of both packages happens only after the full UIKit stack is dev-tested end-to-end. (Settled with the dev.)
- **The conformance suite is owned by this repo**, with a proposed home of `@openzeppelin/adapter-runtime-utils`. It covers the adapter-side contract invariants the UIKit SF-1 invariants doc could not self-enforce: UIKit INV-6 (`forwardVerified` always a concrete boolean), UIKit INV-8 (expected failures return `ok: false`, never throw), UIKit INV-12 (deterministic-input-under-stable-state idempotency, including the deep-equal-under-cache-TTL semantics this repo defines), and UIKit INV-16 (user-safe `provenance.label` — this repo defines the allowlist). This is this repo's commitment back to the UIKit, making UIKit SC-004 / SC-006 enforceable against a real adapter. (Settled with the dev.)
- **ENS v1 and v2 both live in `@openzeppelin/adapter-evm-core`** — the capability file sits alongside `src/capabilities/addressing.ts`. There is definitively no new `adapter-evm-ens-v2` package. The `EnsProvenance` type and `isEnsProvenance` guard are exported from `@openzeppelin/adapter-evm-core`; the UIKit's SF-6 imports them from there. (Settled with the dev.)
- **`scopedToNetworkId` values use this repo's internal `NetworkConfig.networkId` namespace.** If a concrete case forces CAIP-2 (or a similar external namespace) instead, that is a drift note to raise at the relevant sub-feature's Design stage — not a default taken here. (Settled with the dev.)
- **`@openzeppelin/ui-types` provides the locked shape as a workspace/peer dependency (currently `^3.1.0`).** This repo implements against UIKit SF-1's exported interface, value types, and error union without modifying them; a downstream types-package minor bump (post UIKit SF-1) is consumed via the local-linking flow above, not a published release, during development.
- **ENS support targets EVM adapters only for this initiative.** Non-EVM adapters omit the capability entirely (optional at the runtime-map level) and must not break. Other name systems (SNS, Unstoppable, `.sui`, Aptos) are follow-up initiatives.
- **All sub-features are `work-type: service`.** These are headless off-chain primitives (viem / RPC / gateway integration and a test harness library), not UI. The adapter's React glue is not in scope for this initiative — UI consumption lives in the UIKit repo.
- **Stakes rationale** — SF-2, SF-3, SF-5 are High because a wrong resolution (or a mis-reported `forwardVerified`) carries fund-loss / spoofing risk; SF-4 is High because it defines novel correctness semantics and is the enforceable fund-safety gate (and a cross-adapter reusable artifact, so Docs earns its place). SF-1 is Medium — a miscoded error is observable and recoverable, not fund-loss — but above Low because it centralizes the never-throw contract every other SF relies on.

## Edge Cases _(include if any non-obvious cases exist; remove otherwise)_

- **Forward / reverse record mismatch** — a reverse record points to `alice.eth`, but `alice.eth`'s forward record points to a *different* address. Per Approach A the adapter suppresses the name and folds to `ADDRESS_NOT_FOUND` (SF-3 scenario 2) — it never surfaces a forward-mismatched name; anti-spoofing is preserved at the UIKit SF-4 display layer (truncated hex).
- **Chain-scoped (cross-chain) v2 addresses** — an ENS v2 name resolves via `coinType` (ENSIP-9/11/19) to an address meaningful on a different chain than the L1 mainnet the lookup runs on. The result's provenance must carry the network scope so the consumer binds it to the correct chain (SF-5 scenario 1); a chain-scoped address must never be presented as a plain L1-mainnet address.
- **Gateway unreachable vs name-not-found** — a v2 CCIP-Read gateway failure must map to `EXTERNAL_GATEWAY_ERROR`, distinct from `NAME_NOT_FOUND`, and must never silently fall back to a stale v1-only lookup (SF-5 scenario 2).
- **Deterministic-under-stable-state vs cache TTL** — repeated calls with identical input under stable underlying state must return structurally-equal results; the conformance harness defines what "structurally equal under a cache TTL" means so a memoizing adapter and a re-querying adapter both satisfy it (SF-4).
- **Non-EVM adapter present in the same workspace** — the additive capability must not force any non-EVM adapter to implement or stub it; omission at the runtime-map level is the supported path (SF-2 scenario 3, SC-006).
- **Non-user-safe `label`** — an adapter that emits an internal identifier or gateway URL as `provenance.label` must be caught by the conformance harness's allowlist check (SF-4 scenario 2).

## Out of Scope _(mandatory — at minimum, state "None")_

- **The capability type shape** (interface, value types, error union) — owned by the UIKit initiative (UIKit SF-1); this repo implements against it and does not modify it.
- **React hooks, input validation, address display, AddressBookWidget, v2 UX plumbing** — owned by the UIKit initiative (UIKit SF-2..SF-6).
- **Non-EVM name systems** (SNS / Solana, Unstoppable Domains, `.sui`, Aptos) — non-EVM adapters omit the capability; normalization is a follow-up initiative.
- **The adapter's React / wallet-connector glue** — this initiative delivers headless resolution primitives (`work-type: service`) only.
- **Persisted (disk / IndexedDB) resolution cache** — any caching is in-memory and adapter-internal; a persistence layer is a follow-up if integrator feedback demands it. (The UIKit owns consumer-side caching in its hooks.)
- **The `provenance.external` → v2-mechanism semantic boundary** — deliberately unresolved (see Open Questions); the UIKit dev decides it at their SF-6 Design.

## Dev Notes _(optional)_

- **Cross-repo `SF-N` disambiguation.** The two initiatives (this repo and `openzeppelin-ui`) number their sub-features **independently** — this repo's SF-2 is not the UIKit's SF-2. Throughout this artifact, a bare `SF-N` refers to *this repo's* sub-features; every reference to the sibling initiative is written explicitly as `UIKit SF-N`. Downstream stages should preserve this convention.
- **Ground truth read before decomposition:** the UIKit's `artifacts/001-ens-uikit-support/00-specify.md`, `sf-1-name-resolution-capability/02-design.md` (the exact interface/value types this repo implements against), and `.../03-invariants.md` (UIKit INV-1..INV-22; UIKit INV-6/8/12/16 are the adapter-side contract SF-4 enforces). Per pipeline convention these are input, not decisions — downstream stages should read the current UIKit artifacts directly rather than lifting type signatures or values from here.
- **Sibling repo pattern to follow:** `packages/adapter-evm-core/src/capabilities/addressing.ts` is the sibling capability (Tier-1, synchronous). Name resolution is Tier-2 (async, network-scoped, `RuntimeCapability`-extending per the UIKit design), so it will not mirror `addressing.ts` exactly — but the capability-factory + runtime-registration wiring is the pattern to reuse.
- **`viem` is already a dependency of `adapter-evm-core`**, and `@openzeppelin/adapter-runtime-utils` already houses `runtime-capability.ts` / `runtime-factories.ts` — the natural home for the SF-4 conformance harness.
- **Research directive (binds SF-2 and SF-5): prefer `viem` over reinventing ENS.** The Research stages of SF-2 (v1 forward/reverse) and SF-5 (ENS v2) MUST first evaluate `viem`'s built-in ENS support — `getEnsAddress` / `getEnsName` / `getEnsAvatar`, ENSIP-10 wildcard resolution, and CCIP-Read (offchain gateway) handling — before considering any hand-rolled resolver, registry ABI calls, or bespoke gateway client. `viem` is already a dependency, so adopting it adds no new package. Research should confirm: coverage of the v2 / cross-chain paths this initiative needs (the directive originally read "v2 / Namechain / cross-chain"; Namechain was cancelled and is dropped — see Plan Revision 1), the shape of `viem`'s native errors (input to SF-1's error-mapping layer), and any gaps that would still require custom code. A go/no-go on "`viem` covers it" (with the specific gaps enumerated) is a required output of SF-2 and SF-5 Research; reinventing any part `viem` already provides needs an explicit justification recorded there.
- The UIKit design sketches an illustrative `EnsProvenance` / `isEnsProvenance` shape and an adapter `createNameResolution` sketch — these are *sketches* in the UIKit artifact, not delivered there; their real design is this initiative's Design stage (SF-2 for the scaffold, SF-5 for the ENS extension).

## Open Questions _(optional, max 3)_

1. **Mapping of `provenance.external: boolean` onto the v2 mechanisms (registry / CCIP-Read)** — deliberately left unresolved. The UIKit dev decides this at their SF-6 Design; this initiative must not pin a boundary contract for it. Recorded against SF-5. If a concrete SF-5 implementation forces a provisional choice, surface it as a drift note back to the UIKit initiative rather than committing it unilaterally.

## Plan Revisions _(empty on initial run; populated on extension-mode runs)_

*Each extension-mode Specify run appends a dated entry here, leaving the original plan and prior revisions visible above.*

### Revision 1 — 2026-07-04
- **Change:** Reframed **SF-5** from "ENS v2 (CCIP-Read / Namechain L2 registry / cross-chain)" to "ENS v2 = **L1-only**: CCIP-Read / offchain-gateway + cross-chain via `coinType` (ENSIP-9/11/19)". Removed the Namechain-L2-registry framing from the SF-5 heading + Purpose, the US-4 story, and the "Chain-scoped v2 addresses" Edge Case; recorded that the adapter-side `EnsProvenance` extension drops the `'namechain'` mechanism value.
- **Why:** ENS Labs cancelled the Namechain L2 (Feb–Mar 2026); ENS v2 is L1-only. Surfaced by SF-5 Research as the PRIMARY DRIFT; settled cross-repo with the dev + the UIKit orchestrator.
- **Impact:** SF-5 scope only. Base `ResolutionProvenance` (`label` / `external` / `scopedToNetworkId`) is **UNCHANGED** → no SF-1 capability-contract change; completed SF-1 / SF-2 unaffected. DAG, stakes, and dependencies unchanged (SF-5 still depends on SF-1, SF-2; the DAG note carried no Namechain framing, so it needed no edit). Chain-scoping is now via `coinType`, not an L2. Residual "Namechain" mentions intentionally left (outside this run's edit scope) in the Summary, Within-Repo Scope, the Open-Questions v2-mechanism line, and the SF-2/SF-5 Research Dev Note — flagged for a follow-up cleanup pass.
- **Trigger stage:** SF-5 Research (`sf-5-ens-v2/01-research.md`, 2026-07-04).

### Revision 2 — 2026-07-04
- **Change:** Reconciled **SF-3 acceptance scenario 2** (and the matching "Forward / reverse record mismatch" Edge Case) to **Approach A (suppress-on-mismatch)**: a forward-mismatch now folds to `ADDRESS_NOT_FOUND` — the adapter never surfaces a mismatched name — instead of returning the name with `forwardVerified: false`. `forwardVerified` stays a concrete boolean, constant-`true` on any returned name (satisfies SC-003); anti-spoofing is preserved at the UIKit SF-4 display layer (truncated hex).
- **Why:** UIKit chose suppress-on-mismatch for their SF-4 display, and SF-3 Research/Design found viem's `getEnsName` forward-verifies inside the Universal Resolver and never exposes a mismatched name — so Approach A is also the viem-native behavior (the SF-3 Design step-back suggestion). Settled cross-repo with the dev + the UIKit orchestrator.
- **Impact:** SF-3 acceptance scenario 2 + the mismatch Edge Case only; SF-3 Purpose / stakes and the "`forwardVerified` is a concrete boolean" contract are unchanged, as is the DAG. NOTE: **US-2's Independent Test** still references the old "confirm `forwardVerified: false`" reverse path — left untouched (this run's edit scope is US-4 only); flagged for reconciliation.
- **Trigger stage:** SF-3 Design step-back (`sf-3-reverse-resolution/02-design.md`, 2026-07-04) + the UIKit SF-4 decision.

### Revision 3 — 2026-07-04
- **Change:** Residual-consistency cleanup closing the items flagged in Revisions 1 & 2 (they sat outside those runs' edit scope). (a) **Summary** + **Within-Repo Scope** in-scope bullet reframed "ENS v2 (CCIP-Read / Namechain / cross-chain)" → "ENS v2 (L1-only): CCIP-Read + cross-chain via `coinType`". (b) **Open Questions** v2-mechanism enumeration dropped "Namechain" (now "registry / CCIP-Read") — the Open Q itself stays OPEN, still deferred to UIKit SF-6. (c) **US-2 Independent Test** reconciled to Approach A: a forward-mismatch yields `ADDRESS_NOT_FOUND` (no name surfaced) and `forwardVerified` is constant-`true` on any returned name (was "confirm `forwardVerified: false`"). (d) The SF-2/SF-5 Research Dev Note kept as a historical record but annotated that Namechain was dropped (points to Revision 1).
- **Why:** Close the honestly-flagged residuals from Revisions 1 & 2 so the whole document is internally consistent with the SF-5 L1-only reframe and the SF-3 Approach-A decision. Item (c) matters because SF-3 Invariants/Tests read US-2.
- **Impact:** Wording-only; no SF scope, DAG, stakes, dependency, or capability-contract change. Build Status matrix and Last-update log untouched.
- **Trigger stage:** Specify follow-up cleanup (this run), closing the Revision 1 & 2 residuals.
