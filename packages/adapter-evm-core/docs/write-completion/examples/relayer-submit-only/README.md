# Example: Relayer top-level submit-only options

Shows the **forward public contract**: typed `WriteCompletionOptions` on
`RelayerExecutionConfig.transactionOptions`.

```bash
pnpm exec tsc --noEmit submit-only-options.ts
```

Requires `@openzeppelin/ui-types@3.5.0`.
